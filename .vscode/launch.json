{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "OpenOCD Debug",
      "type": "cortex-debug",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "executable": "${workspaceFolder}/build/debug/firmware.elf",
      "servertype": "openocd",
      "interface": "swd",
      "runToEntryPoint": "main",
      "gdbPath": "/usr/bin/gdb-multiarch",

      "preLaunchTask": "Build",
      "configFiles": [
        "interface/stlink.cfg",
        "target/stm32f1x.cfg" /* change to the correct target */
      ],
      "svdFile": "${workspaceFolder}/tools/svd/STM32F101.svd", /* change to the correct target */
      "liveWatch": {
        "enabled": true,
        "samplesPerSecond": 20
      }
    }
  ]
}